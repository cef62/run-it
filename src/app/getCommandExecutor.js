/* @flow */
// import { echo } from 'shelljs'

export default function getCommandExecutor(target: string): string {
  // TODO: search through scaffolded commands for the requested module

  // FIXME: currently simply exec the given command as it is
  return target
}
